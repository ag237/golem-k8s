apiVersion: v1
kind: Service
metadata:
  name: {{ include "golem-k8s.fullname" . }}
  labels:
    {{- include "golem-k8s.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "golem-k8s.selectorLabels" . | nindent 4 }}
